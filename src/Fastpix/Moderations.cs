/* 
 * This file is auto-generated.
 * Do not edit this file manually.
 * Your changes will be overwritten during the next generation.
 */
#nullable enable
namespace Fastpix
{
    using Fastpix.Hooks;
    using Fastpix.Models.Components;
    using Fastpix.Models.Errors;
    using Fastpix.Models.Requests;
    using Fastpix.Utils;
    using Fastpix.Utils.Retries;
    using Newtonsoft.Json;
    using System;
    using System.Collections.Generic;
    using System.Net.Http;
    using System.Net.Http.Headers;
    using System.Threading;
    using System.Threading.Tasks;

    public interface IModerations
    {

        /// <summary>
        /// Enable video moderation
        /// 
        /// <remarks>
        /// This endpoint enables moderation features, such as NSFW and profanity filtering, to detect inappropriate content in existing media.<br/>
        /// <br/>
        /// #### How it works<br/>
        /// 1. Make a `PATCH` request to this endpoint, replacing `&lt;mediaId&gt;` with the ID of the media you want to update.<br/>
        /// 2. Include the `moderation` object and provide the requried `type` parameter in the request body to specify the media type (for example, video/audio/av).<br/>
        /// 4. The response contains the updated media data, confirming the changes made.<br/>
        /// <br/>
        /// You can use the &lt;a href=&quot;https://fastpix.com/docs/ai-events/in-video-ai-events#videomediaaimoderationready&quot;&gt;video.mediaAI.moderation.ready&lt;/a&gt; webhook event to track and notify about the detected moderation results.<br/>
        /// <br/>
        /// **Use case:** This is particularly useful when a user uploads a video and later decides to enable moderation detection without the need to re-upload it.<br/>
        /// <br/>
        /// Related guide: &lt;a href=&quot;https://fastpix.com/docs/video-intelligence/detect-nsfw-content-and-profanity&quot;&gt;Moderate NSFW &amp; Profanity&lt;/a&gt;<br/>
        /// 
        /// </remarks>
        /// </summary>
        Task<UpdateMediaModerationResponse> UpdateAsync(string mediaId, UpdateMediaModerationRequestBody body, RetryConfig? retryConfig = null, CancellationToken? cancellationToken = null);
    }

    public class Moderations: IModerations
    {
        public SdkConfig SDKConfiguration { get; private set; }

        private const string ApplicationJsonContentType = "application/json";
        private const string UnknownContentTypeError = "Unknown content type received";
        private const string ApiErrorOccurred = "API error occurred";

        public Moderations(SdkConfig config)
        {
            SDKConfiguration = config;
        }

        private RetryConfig ResolveRetryConfig(RetryConfig? retryConfig)
        {
            if (retryConfig != null)
            {
                return retryConfig;
            }

            if (this.SDKConfiguration.RetryConfig != null)
            {
                return this.SDKConfiguration.RetryConfig;
            }

            var backoff = new BackoffStrategy(
                initialIntervalMs: 1000L,
                maxIntervalMs: 10000L,
                maxElapsedTimeMs: 3600000L,
                exponent: 1.5
            );
            return new RetryConfig(
                strategy: RetryConfig.RetryStrategy.BACKOFF,
                backoff: backoff,
                retryConnectionErrors: true
            );
        }

        private static List<string> GetRetryStatusCodes()
        {
            return new List<string>
            {
                "408",
                "429",
                "500",
                "502",
                "503",
                "504",
            };
        }

        private async Task<HttpResponseMessage> SendWithRetriesAsync(HttpRequestMessage httpRequest, RetryConfig retryConfig, HookContext hookCtx, CancellationToken? cancellationToken)
        {
            Func<Task<HttpResponseMessage>> retrySend = async () =>
            {
                var _httpRequest = await SDKConfiguration.Client.CloneAsync(httpRequest);
                return await SDKConfiguration.Client.SendAsync(_httpRequest, cancellationToken);
            };
            var retries = new Fastpix.Utils.Retries.Retries(retrySend, retryConfig, GetRetryStatusCodes());

            HttpResponseMessage httpResponse;
            try
            {
                httpResponse = await retries.Run();
                int _statusCode = (int)httpResponse.StatusCode;

                if (_statusCode >= 400 && _statusCode < 600)
                {
                    var _httpResponse = await this.SDKConfiguration.Hooks.AfterErrorAsync(new AfterErrorContext(hookCtx), httpResponse, null);
                    if (_httpResponse != null)
                    {
                        httpResponse = _httpResponse;
                    }
                }
            }
            catch (Exception error)
            {
                var _httpResponse = await this.SDKConfiguration.Hooks.AfterErrorAsync(new AfterErrorContext(hookCtx), null, error);
                if (_httpResponse == null)
                {
                    throw;
                }
                httpResponse = _httpResponse;
            }

            return await this.SDKConfiguration.Hooks.AfterSuccessAsync(new AfterSuccessContext(hookCtx), httpResponse);
        }

        private static T DeserializeBody<T>(string httpResponseBody, HttpRequestMessage httpRequest, HttpResponseMessage httpResponse, string typeName, NullValueHandling nullValueHandling)
        {
            try
            {
                return ResponseBodyDeserializer.DeserializeNotNull<T>(httpResponseBody, nullValueHandling);
            }
            catch (Exception ex)
            {
                throw new ResponseValidationException($"Failed to deserialize response body into {typeName}.", httpRequest, httpResponse, httpResponseBody, ex);
            }
        }

        public async Task<UpdateMediaModerationResponse> UpdateAsync(string mediaId, UpdateMediaModerationRequestBody body, RetryConfig? retryConfig = null, CancellationToken? cancellationToken = null)
        {
            var request = new UpdateMediaModerationRequest()
            {
                MediaId = mediaId,
                Body = body,
            };
            string baseUrl = this.SDKConfiguration.GetTemplatedServerUrl();
            var urlString = UrlBuilder.Build(baseUrl, "/on-demand/{mediaId}/moderation", request, null);

            var httpRequest = new HttpRequestMessage(HttpMethod.Patch, urlString);
            httpRequest.Headers.Add("user-agent", SDKConfiguration.UserAgent);

            var serializedBody = RequestBodySerializer.Serialize(request, "Body", "json", false, false);
            if (serializedBody != null)
            {
                httpRequest.Content = serializedBody;
            }

            if (SDKConfiguration.SecuritySource != null)
            {
                httpRequest = new SecurityMetadata(SDKConfiguration.SecuritySource).Apply(httpRequest);
            }

            var hookCtx = new HookContext(SDKConfiguration, baseUrl, "update-media-moderation", null, SDKConfiguration.SecuritySource, cancellationToken);

            httpRequest = await this.SDKConfiguration.Hooks.BeforeRequestAsync(new BeforeRequestContext(hookCtx), httpRequest);
            retryConfig = ResolveRetryConfig(retryConfig);

            HttpResponseMessage httpResponse = await SendWithRetriesAsync(httpRequest, retryConfig, hookCtx, cancellationToken);

            var contentType = httpResponse.Content.Headers.ContentType?.MediaType;
            int responseStatusCode = (int)httpResponse.StatusCode;
            if(responseStatusCode == 200)
            {
                if(Utilities.IsContentTypeMatch(ApplicationJsonContentType, contentType))
                {
                    var httpResponseBody = await httpResponse.Content.ReadAsStringAsync();
                    var obj = DeserializeBody<UpdateMediaModerationResponseBody>(httpResponseBody, httpRequest, httpResponse, "UpdateMediaModerationResponseBody", NullValueHandling.Ignore);

                    var response = new UpdateMediaModerationResponse()
                    {
                        HttpMeta = new Models.Components.HttpMetadata()
                        {
                            Response = httpResponse,
                            Request = httpRequest
                        }
                    };
                    response.Object = obj;
                    return response;
                }

                throw new Models.Errors.ApiException(UnknownContentTypeError, httpRequest, httpResponse, await httpResponse.Content.ReadAsStringAsync());
            }
            else if(responseStatusCode >= 400 && responseStatusCode < 600)
            {
                throw new Models.Errors.ApiException(ApiErrorOccurred, httpRequest, httpResponse, await httpResponse.Content.ReadAsStringAsync());
            }
            else
            {
                if(Utilities.IsContentTypeMatch(ApplicationJsonContentType, contentType))
                {
                    var httpResponseBody = await httpResponse.Content.ReadAsStringAsync();
                    var obj = DeserializeBody<DefaultError>(httpResponseBody, httpRequest, httpResponse, nameof(DefaultError), NullValueHandling.Ignore);

                    var response = new UpdateMediaModerationResponse()
                    {
                        HttpMeta = new Models.Components.HttpMetadata()
                        {
                            Response = httpResponse,
                            Request = httpRequest
                        }
                    };
                    response.DefaultError = obj;
                    return response;
                }

                throw new Models.Errors.ApiException(UnknownContentTypeError, httpRequest, httpResponse, await httpResponse.Content.ReadAsStringAsync());
            }
        }
    }
}