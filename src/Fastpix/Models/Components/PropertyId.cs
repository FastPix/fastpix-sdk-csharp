/* 
 * This file is auto-generated.
 * Do not edit this file manually.
 * Your changes will be overwritten during the next generation.
 */
#nullable enable
namespace Fastpix.Models.Components
{
    using Fastpix.Utils;
    using Newtonsoft.Json;
    using Newtonsoft.Json.Linq;
    using System;
    using System.Collections.Generic;
    using System.Numerics;
    using System.Reflection;

    public class PropertyIdType
    {
        private PropertyIdType(string value) { Value = value; }

        public string Value { get; private set; }

        public static PropertyIdType Str { get { return new PropertyIdType("str"); } }

        public static PropertyIdType Integer { get { return new PropertyIdType("integer"); } }

        public static PropertyIdType Null { get { return new PropertyIdType("null"); } }

        public override string ToString() { return Value; }
        public static implicit operator String(PropertyIdType v) { return v.Value; }
        public static PropertyIdType FromString(string v) {
            switch(v) {
                case "str": return Str;
                case "integer": return Integer;
                case "null": return Null;
                default: throw new ArgumentException("Invalid value for PropertyIdType");
            }
        }
        public override bool Equals(object? obj)
        {
            if (obj == null || GetType() != obj.GetType())
            {
                return false;
            }
            return Value.Equals(((PropertyIdType)obj).Value);
        }

        public override int GetHashCode()
        {
            return Value.GetHashCode();
        }
    }


    /// <summary>
    /// Represents the unique identifier assigned to a FastPix property, which is associated with a specific workspace or project. It helps link playback and analytics data to the correct property configuration.<br/>
    /// 
    /// <remarks>
    /// 
    /// </remarks>
    /// </summary>
    [JsonConverter(typeof(PropertyId.PropertyIdConverter))]
    public class PropertyId
    {
        public PropertyId(PropertyIdType type)
        {
            Type = type;
        }

        [FastpixMetadata("form:explode=true")]
        public string? Str { get; set; }

        [FastpixMetadata("form:explode=true")]
        public long? Integer { get; set; }

        public PropertyIdType Type { get; set; }
        public static PropertyId CreateStr(string str)
        {
            PropertyIdType typ = PropertyIdType.Str;

            PropertyId res = new PropertyId(typ);
            res.Str = str;
            return res;
        }
        public static PropertyId CreateInteger(long integer)
        {
            PropertyIdType typ = PropertyIdType.Integer;

            PropertyId res = new PropertyId(typ);
            res.Integer = integer;
            return res;
        }

        public static PropertyId CreateNull()
        {
            PropertyIdType typ = PropertyIdType.Null;
            return new PropertyId(typ);
        }

        public class PropertyIdConverter : JsonConverter
        {
            public override bool CanConvert(System.Type objectType) => objectType == typeof(PropertyId);

            public override bool CanRead => true;

            public override object? ReadJson(JsonReader reader, System.Type objectType, object? existingValue, JsonSerializer serializer)
            {
                if (reader.TokenType == JsonToken.Null)
                {
                    return null;
                }

                var json = JRaw.Create(reader).ToString();
                var fallbackCandidates = new List<(System.Type, object, string)>();

                if (json[0] == '"' && json[^1] == '"'){
                    return new PropertyId(PropertyIdType.Str)
                    {
                        Str = json[1..^1]
                    };
                }

                try
                {
                    var converted = Convert.ToInt64(json);
                    return new PropertyId(PropertyIdType.Integer)
                    {
                        Integer = converted
                    };
                }
                catch (System.FormatException)
                {
                    // try next option
                }

                if (fallbackCandidates.Count > 0)
                {
                    fallbackCandidates.Sort((a, b) => ResponseBodyDeserializer.CompareFallbackCandidates(a.Item1, b.Item1, json));
                    foreach(var (deserializationType, returnObject, propertyName) in fallbackCandidates)
                    {
                        try
                        {
                            return ResponseBodyDeserializer.DeserializeUndiscriminatedUnionFallback(deserializationType, returnObject, propertyName, json);
                        }
                        catch (ResponseBodyDeserializer.DeserializationException)
                        {
                            // try next fallback option
                        }
                        catch (Exception)
                        {
                            throw;
                        }
                    }
                }

                throw new InvalidOperationException("Could not deserialize into any supported types.");
            }

            public override void WriteJson(JsonWriter writer, object? value, JsonSerializer serializer)
            {
                if (value == null)
                {
                    writer.WriteRawValue("null");
                    return;
                }

                PropertyId res = (PropertyId)value;
                if (PropertyIdType.FromString(res.Type).Equals(PropertyIdType.Null))
                {
                    writer.WriteRawValue("null");
                    return;
                }

                if (res.Str != null)
                {
                    writer.WriteRawValue(Utilities.SerializeJSON(res.Str));
                    return;
                }

                if (res.Integer != null)
                {
                    writer.WriteRawValue(Utilities.SerializeJSON(res.Integer));
                    return;
                }
            }

        }

    }
}