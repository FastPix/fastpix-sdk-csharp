/* 
 * This file is auto-generated.
 * Do not edit this file manually.
 * Your changes will be overwritten during the next generation.
 */
#nullable enable
namespace Fastpix.Models.Components
{
    using Fastpix.Utils;
    using Newtonsoft.Json;
    using Newtonsoft.Json.Linq;
    using System;
    using System.Collections.Generic;
    using System.Numerics;
    using System.Reflection;

    public class ViewerTimeType
    {
        private ViewerTimeType(string value) { Value = value; }

        public string Value { get; private set; }

        public static ViewerTimeType Str { get { return new ViewerTimeType("str"); } }

        public static ViewerTimeType Integer { get { return new ViewerTimeType("integer"); } }

        public static ViewerTimeType Null { get { return new ViewerTimeType("null"); } }

        public override string ToString() { return Value; }
        public static implicit operator String(ViewerTimeType v) { return v.Value; }
        public static ViewerTimeType FromString(string v) {
            switch(v) {
                case "str": return Str;
                case "integer": return Integer;
                case "null": return Null;
                default: throw new ArgumentException("Invalid value for ViewerTimeType");
            }
        }
        public override bool Equals(object? obj)
        {
            if (obj == null || GetType() != obj.GetType())
            {
                return false;
            }
            return Value.Equals(((ViewerTimeType)obj).Value);
        }

        public override int GetHashCode()
        {
            return Value.GetHashCode();
        }
    }


    /// <summary>
    /// The unix epoch timestamp which represents the actual time the event has occurred.<br/>
    /// 
    /// <remarks>
    /// 
    /// </remarks>
    /// </summary>
    [JsonConverter(typeof(ViewerTime.ViewerTimeConverter))]
    public class ViewerTime
    {
        public ViewerTime(ViewerTimeType type)
        {
            Type = type;
        }

        [FastpixMetadata("form:explode=true")]
        public string? Str { get; set; }

        [FastpixMetadata("form:explode=true")]
        public long? Integer { get; set; }

        public ViewerTimeType Type { get; set; }
        public static ViewerTime CreateStr(string str)
        {
            ViewerTimeType typ = ViewerTimeType.Str;

            ViewerTime res = new ViewerTime(typ);
            res.Str = str;
            return res;
        }
        public static ViewerTime CreateInteger(long integer)
        {
            ViewerTimeType typ = ViewerTimeType.Integer;

            ViewerTime res = new ViewerTime(typ);
            res.Integer = integer;
            return res;
        }

        public static ViewerTime CreateNull()
        {
            ViewerTimeType typ = ViewerTimeType.Null;
            return new ViewerTime(typ);
        }

        public class ViewerTimeConverter : JsonConverter
        {
            public override bool CanConvert(System.Type objectType) => objectType == typeof(ViewerTime);

            public override bool CanRead => true;

            public override object? ReadJson(JsonReader reader, System.Type objectType, object? existingValue, JsonSerializer serializer)
            {
                if (reader.TokenType == JsonToken.Null)
                {
                    return null;
                }

                var json = JRaw.Create(reader).ToString();
                var fallbackCandidates = new List<(System.Type, object, string)>();

                if (json[0] == '"' && json[^1] == '"'){
                    return new ViewerTime(ViewerTimeType.Str)
                    {
                        Str = json[1..^1]
                    };
                }

                try
                {
                    var converted = Convert.ToInt64(json);
                    return new ViewerTime(ViewerTimeType.Integer)
                    {
                        Integer = converted
                    };
                }
                catch (System.FormatException)
                {
                    // try next option
                }

                if (fallbackCandidates.Count > 0)
                {
                    fallbackCandidates.Sort((a, b) => ResponseBodyDeserializer.CompareFallbackCandidates(a.Item1, b.Item1, json));
                    foreach(var (deserializationType, returnObject, propertyName) in fallbackCandidates)
                    {
                        try
                        {
                            return ResponseBodyDeserializer.DeserializeUndiscriminatedUnionFallback(deserializationType, returnObject, propertyName, json);
                        }
                        catch (ResponseBodyDeserializer.DeserializationException)
                        {
                            // try next fallback option
                        }
                        catch (Exception)
                        {
                            throw;
                        }
                    }
                }

                throw new InvalidOperationException("Could not deserialize into any supported types.");
            }

            public override void WriteJson(JsonWriter writer, object? value, JsonSerializer serializer)
            {
                if (value == null)
                {
                    writer.WriteRawValue("null");
                    return;
                }

                ViewerTime res = (ViewerTime)value;
                if (ViewerTimeType.FromString(res.Type).Equals(ViewerTimeType.Null))
                {
                    writer.WriteRawValue("null");
                    return;
                }

                if (res.Str != null)
                {
                    writer.WriteRawValue(Utilities.SerializeJSON(res.Str));
                    return;
                }

                if (res.Integer != null)
                {
                    writer.WriteRawValue(Utilities.SerializeJSON(res.Integer));
                    return;
                }
            }

        }

    }
}