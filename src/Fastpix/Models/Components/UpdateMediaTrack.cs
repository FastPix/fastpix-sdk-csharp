/* 
 * This file is auto-generated.
 * Do not edit this file manually.
 * Your changes will be overwritten during the next generation.
 */
#nullable enable
namespace Fastpix.Models.Components
{
    using Fastpix.Models.Components;
    using Fastpix.Utils;
    using Newtonsoft.Json;
    using Newtonsoft.Json.Linq;
    using System;
    using System.Collections.Generic;
    using System.Numerics;
    using System.Reflection;

    public class UpdateMediaTrackType
    {
        private UpdateMediaTrackType(string value) { Value = value; }

        public string Value { get; private set; }

        public static UpdateMediaTrackType VideoTrack { get { return new UpdateMediaTrackType("VideoTrack"); } }

        public static UpdateMediaTrackType AudioTrack { get { return new UpdateMediaTrackType("AudioTrack"); } }

        public static UpdateMediaTrackType SubtitleTrack { get { return new UpdateMediaTrackType("SubtitleTrack"); } }

        public override string ToString() { return Value; }
        public static implicit operator String(UpdateMediaTrackType v) { return v.Value; }
        public static UpdateMediaTrackType FromString(string v) {
            switch(v) {
                case "VideoTrack": return VideoTrack;
                case "AudioTrack": return AudioTrack;
                case "SubtitleTrack": return SubtitleTrack;
                default: throw new ArgumentException("Invalid value for UpdateMediaTrackType");
            }
        }
        public override bool Equals(object? obj)
        {
            if (obj == null || GetType() != obj.GetType())
            {
                return false;
            }
            return Value.Equals(((UpdateMediaTrackType)obj).Value);
        }

        public override int GetHashCode()
        {
            return Value.GetHashCode();
        }
    }


    [JsonConverter(typeof(UpdateMediaTrack.UpdateMediaTrackConverter))]
    public class UpdateMediaTrack
    {
        public UpdateMediaTrack(UpdateMediaTrackType type)
        {
            Type = type;
        }

        [FastpixMetadata("form:explode=true")]
        public VideoTrack? VideoTrack { get; set; }

        [FastpixMetadata("form:explode=true")]
        public AudioTrack? AudioTrack { get; set; }

        [FastpixMetadata("form:explode=true")]
        public SubtitleTrack? SubtitleTrack { get; set; }

        public UpdateMediaTrackType Type { get; set; }
        public static UpdateMediaTrack CreateVideoTrack(VideoTrack videoTrack)
        {
            UpdateMediaTrackType typ = UpdateMediaTrackType.VideoTrack;

            UpdateMediaTrack res = new UpdateMediaTrack(typ);
            res.VideoTrack = videoTrack;
            return res;
        }
        public static UpdateMediaTrack CreateAudioTrack(AudioTrack audioTrack)
        {
            UpdateMediaTrackType typ = UpdateMediaTrackType.AudioTrack;

            UpdateMediaTrack res = new UpdateMediaTrack(typ);
            res.AudioTrack = audioTrack;
            return res;
        }
        public static UpdateMediaTrack CreateSubtitleTrack(SubtitleTrack subtitleTrack)
        {
            UpdateMediaTrackType typ = UpdateMediaTrackType.SubtitleTrack;

            UpdateMediaTrack res = new UpdateMediaTrack(typ);
            res.SubtitleTrack = subtitleTrack;
            return res;
        }

        public class UpdateMediaTrackConverter : JsonConverter
        {
            public override bool CanConvert(System.Type objectType) => objectType == typeof(UpdateMediaTrack);

            public override bool CanRead => true;

            public override object? ReadJson(JsonReader reader, System.Type objectType, object? existingValue, JsonSerializer serializer)
            {
                if (reader.TokenType == JsonToken.Null)
                {
                    throw new InvalidOperationException("Received unexpected null JSON value");
                }

                var json = JRaw.Create(reader).ToString();
                var fallbackCandidates = new List<(System.Type, object, string)>();

                try
                {
                    return new UpdateMediaTrack(UpdateMediaTrackType.AudioTrack)
                    {
                        AudioTrack = ResponseBodyDeserializer.DeserializeUndiscriminatedUnionMember<AudioTrack>(json)
                    };
                }
                catch (ResponseBodyDeserializer.MissingMemberException)
                {
                    fallbackCandidates.Add((typeof(AudioTrack), new UpdateMediaTrack(UpdateMediaTrackType.AudioTrack), "AudioTrack"));
                }
                catch (ResponseBodyDeserializer.DeserializationException)
                {
                    // try next option
                }
                catch (Exception)
                {
                    throw;
                }

                try
                {
                    return new UpdateMediaTrack(UpdateMediaTrackType.SubtitleTrack)
                    {
                        SubtitleTrack = ResponseBodyDeserializer.DeserializeUndiscriminatedUnionMember<SubtitleTrack>(json)
                    };
                }
                catch (ResponseBodyDeserializer.MissingMemberException)
                {
                    fallbackCandidates.Add((typeof(SubtitleTrack), new UpdateMediaTrack(UpdateMediaTrackType.SubtitleTrack), "SubtitleTrack"));
                }
                catch (ResponseBodyDeserializer.DeserializationException)
                {
                    // try next option
                }
                catch (Exception)
                {
                    throw;
                }

                try
                {
                    return new UpdateMediaTrack(UpdateMediaTrackType.VideoTrack)
                    {
                        VideoTrack = ResponseBodyDeserializer.DeserializeUndiscriminatedUnionMember<VideoTrack>(json)
                    };
                }
                catch (ResponseBodyDeserializer.MissingMemberException)
                {
                    fallbackCandidates.Add((typeof(VideoTrack), new UpdateMediaTrack(UpdateMediaTrackType.VideoTrack), "VideoTrack"));
                }
                catch (ResponseBodyDeserializer.DeserializationException)
                {
                    // try next option
                }
                catch (Exception)
                {
                    throw;
                }

                if (fallbackCandidates.Count > 0)
                {
                    fallbackCandidates.Sort((a, b) => ResponseBodyDeserializer.CompareFallbackCandidates(a.Item1, b.Item1, json));
                    foreach(var (deserializationType, returnObject, propertyName) in fallbackCandidates)
                    {
                        try
                        {
                            return ResponseBodyDeserializer.DeserializeUndiscriminatedUnionFallback(deserializationType, returnObject, propertyName, json);
                        }
                        catch (ResponseBodyDeserializer.DeserializationException)
                        {
                            // try next fallback option
                        }
                        catch (Exception)
                        {
                            throw;
                        }
                    }
                }

                throw new InvalidOperationException("Could not deserialize into any supported types.");
            }

            public override void WriteJson(JsonWriter writer, object? value, JsonSerializer serializer)
            {
                if (value == null)
                {
                    throw new InvalidOperationException("Unexpected null JSON value.");
                }

                UpdateMediaTrack res = (UpdateMediaTrack)value;

                if (res.VideoTrack != null)
                {
                    writer.WriteRawValue(Utilities.SerializeJSON(res.VideoTrack));
                    return;
                }

                if (res.AudioTrack != null)
                {
                    writer.WriteRawValue(Utilities.SerializeJSON(res.AudioTrack));
                    return;
                }

                if (res.SubtitleTrack != null)
                {
                    writer.WriteRawValue(Utilities.SerializeJSON(res.SubtitleTrack));
                    return;
                }
            }

        }

    }
}